import { CheckoutForm } from '@/components/checkout/CheckoutForm';

export const metadata = {
  title: 'Checkout | PROOF OF WEAR',
  description: 'Complete your Bitcoin merch order with Lightning payment.',
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
