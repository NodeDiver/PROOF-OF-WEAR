# 01 - Site Analysis Report

> Generated by Scout Agent | Lightning Payment Pipeline
> Project: PROOF-OF-WEAR
> Date: 2026-01-16

---

## 1. Framework & Stack

| Component | Technology | Version |
|-----------|------------|---------|
| Framework | Next.js (App Router) | 16.1.2 |
| React | React | 19.2.3 |
| Language | TypeScript | ^5 |
| Styling | Tailwind CSS | ^4 |
| State Management | Zustand | ^5.0.10 |
| i18n | next-intl | ^4.7.0 |
| Theme | next-themes | ^0.4.6 |
| Animations | Framer Motion | ^12.26.2 |
| Icons | Lucide React | ^0.562.0 |

### Payment Package (Already Installed)
```json
"@getalby/sdk": "^7.0.0"
```

---

## 2. Project Structure

```
src/
├── app/
│   ├── layout.tsx              # Root layout with providers
│   ├── page.tsx                # Homepage (Hero + Products)
│   ├── not-found.tsx           # 404 page
│   ├── checkout/
│   │   └── page.tsx            # Checkout page
│   ├── product/
│   │   └── [slug]/
│   │       └── page.tsx        # Product detail page
│   └── api/
│       ├── orders/
│       │   └── route.ts        # POST /api/orders
│       └── invoices/
│           └── route.ts        # POST /api/invoices
├── components/
│   ├── Providers.tsx           # Theme + Intl providers
│   ├── ui/                     # Design system (Button, Card, Input, Badge)
│   ├── layout/                 # Header, Footer, ThemeToggle, LanguageToggle
│   ├── cart/                   # CartDrawer
│   ├── checkout/               # CheckoutForm
│   ├── product/                # ProductCard, ProductDetail
│   └── icons/                  # Logo, LogoSquare
├── lib/
│   ├── products.ts             # Product data
│   ├── store.ts                # Zustand cart store
│   └── utils.ts                # Helper functions
├── types/
│   └── index.ts                # TypeScript interfaces
├── i18n/
│   ├── routing.ts              # Locale configuration
│   └── request.ts              # Server-side locale handling
└── messages/
    ├── en.json                 # English translations
    └── es.json                 # Spanish translations
```

---

## 3. Checkout/Cart System

### Cart State Management
- **File**: `src/lib/store.ts`
- **Library**: Zustand with `persist` middleware
- **Storage**: localStorage (key: `pow-cart`)
- **State Shape**:
  ```typescript
  interface CartState {
    items: CartItem[];
    isOpen: boolean;
    addItem, removeItem, updateQuantity, clearCart
    toggleCart, openCart, closeCart
    getItemCount(), getTotal()
  }
  ```

### Cart UI
- **File**: `src/components/cart/CartDrawer.tsx`
- **Features**: Slide-in drawer with Framer Motion, quantity controls

### Checkout Flow
- **Page**: `src/app/checkout/page.tsx`
- **Form**: `src/components/checkout/CheckoutForm.tsx` (lines 44-95)
- **Current Flow**:
  1. User fills shipping form (email, name, address, city, country, postal code)
  2. On submit: `POST /api/orders` → creates order
  3. Then: `POST /api/invoices` → generates invoice
  4. Currently: Shows `alert()` with fake bolt11 (line 85)
  5. Then: `clearCart()` is called

---

## 4. API Routes

### POST /api/orders
- **File**: `src/app/api/orders/route.ts`
- **Input**: `{ items, customer, totalSats }`
- **Output**: `{ id, items, customer, totalSats, status, createdAt }`
- **Status**: Mock (no database persistence)

### POST /api/invoices
- **File**: `src/app/api/invoices/route.ts`
- **Input**: `{ orderId, amountSats }`
- **Output**: `{ invoiceId, orderId, bolt11, paymentHash, amountSats, status, expiresAt, paymentUrl }`
- **Status**: Mock (generates fake bolt11)
- **NWC Ready**: Comments show exact code needed (lines 39-42)

---

## 5. Type Definitions

### File: `src/types/index.ts`

```typescript
// Product Types (lines 7-21)
interface Product { id, slug, name, priceInSats, image, category, sizes?, inStock, featured }

// Cart Types (lines 24-46)
interface CartItem { productId, quantity, size? }
interface CartState { items, actions, computed }

// Customer Types (lines 49-56)
interface CustomerInfo { email, fullName, address, city, country, postalCode }

// Order Types (lines 59-67)
interface Order { id, items, customer, totalSats, status, createdAt, paidAt? }

// Payment Types (lines 69-87)
interface LightningInvoice { invoiceId, bolt11, paymentHash, amountSats, expiresAt, status }
interface PaymentRequest { orderId, amountSats }
interface PaymentResponse { invoice, paymentUrl? }
```

---

## 6. i18n Setup

- **Library**: next-intl v4.7.0
- **Locales**: `en`, `es`
- **Strategy**: Cookie-based (`NEXT_LOCALE`)
- **Files**:
  - `src/messages/en.json`
  - `src/messages/es.json`
- **Toggle**: `src/components/layout/LanguageToggle.tsx`

---

## 7. Design System

### Style: Neo-Brutalist
- Borders: 3px solid
- Shadows: Hard drop shadows (no blur)
- Accent: Bitcoin Orange `#F7931A`
- Dark mode: Full support via next-themes

### Typography
- Display: Bebas Neue
- Body: Space Grotesk
- Mono: Space Mono

---

## 8. Environment Variables

### Current (from code comments):
```env
# Required for NWC
NWC_URL=nostr+walletconnect://...
```

### Not yet configured:
- No `.env.local` in project
- No `.env.example` exists

---

## 9. Known Gaps for Lightning Integration

| Gap | Status | Priority |
|-----|--------|----------|
| Real NWC invoice creation | Mock only | Critical |
| Payment status polling | Not implemented | High |
| QR code display | Not implemented | High |
| Payment success page | Not implemented | High |
| Webhook handling | Not implemented | Medium |
| Test product (1 sat) | Not exists | Medium |
| Database persistence | Mock only | Medium |

---

## 10. Key Files for Modification

| Purpose | File | Lines to Modify |
|---------|------|-----------------|
| Invoice creation | `src/app/api/invoices/route.ts` | 44-57 (replace fake with real NWC) |
| Checkout submit | `src/components/checkout/CheckoutForm.tsx` | 81-88 (add payment modal) |
| Types | `src/types/index.ts` | 69-87 (extend payment types) |
| Products | `src/lib/products.ts` | Add test product |
| New API | `src/app/api/invoices/[id]/status/route.ts` | Create new |
| New component | `src/components/checkout/PaymentModal.tsx` | Create new |

---

## Summary

PROOF-OF-WEAR is a **Next.js 16 + React 19** e-commerce site with:
- Complete cart/checkout flow (Zustand)
- Mock Lightning invoice generation (ready for NWC)
- `@getalby/sdk` already installed
- Neo-brutalist design system
- Bilingual support (en/es)

**Ready for Phase 2**: Architecture mapping to identify exact integration points.
